package jfml;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.fail;

public class JFMLTest {

    @Test
    public void checkTest() {
        // Si la línea siguiente se descomenta, el test falla.
        // Al hacer mvn clean install se lanzan todos los test, de modo que también fallaría.
        // fail("This was supposed to happen :).");
    }
}
